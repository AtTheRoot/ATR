import React from "react"
import styled from "@emotion/styled"


const PillContainer = styled.h1``


const Pill = (text) => {
    return (
        <PillContainer>{text}</PillContainer>
    )
}

export default Pill